import React from "react";
import { TodoApp } from "todo/todo";

const TodoComponent = () => {
  return (
    <div>
      <TodoApp></TodoApp>
    </div>
  );
};

export default TodoComponent; // Exportieren Sie die Komponente als Standardexport
